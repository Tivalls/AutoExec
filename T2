-- Main Payload Script (T2 - Wrapped in Function)

local script_key, currentHwid, baseApiUrlFromLoader = ... -- Arguments passed
print(string.format("[PAYLOAD DEBUG] Received args: key=%s | hwid=%s | apiUrl=%s", tostring(script_key), tostring(currentHwid), tostring(baseApiUrlFromLoader)))

local AUTH_API_BASE_URL = baseApiUrlFromLoader or "YOUR_API_ENDPOINT_BASE_URL_HERE" -- !! REPLACE PLACEHOLDER !!
print("[PAYLOAD DEBUG] Using AUTH_API_BASE_URL:", AUTH_API_BASE_URL)

-- Services (Define outside main function for clarity)
local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local HttpService = game:GetService("HttpService")
local localPlayer = Players.LocalPlayer

-- Main function to be returned and executed by the loader
local function T2_Main()
    print("[PAYLOAD] T2_Main function executing...") -- Log entry

    -- Argument Check (Warn only)
    if not script_key or not currentHwid then warn("[PAYLOAD WARNING] Key or HWID argument was nil/false.") end
    if not AUTH_API_BASE_URL or AUTH_API_BASE_URL == "YOUR_API_ENDPOINT_BASE_URL_HERE" then warn("[PAYLOAD WARNING] AUTH_API_BASE_URL was nil or still set to placeholder!") end
    print("[PAYLOAD] Payload script running...")

    -- Define nested functions or keep them outside
    local monitorActive = true
    local flaggedWords = { /* ... your full list ... */ }
    local webhook_url = "https://discord.com/api/webhooks/1325735244942409809/CMVN3_2Cpi92tSlT9O_bLdv_GZrWvYkY2nOTTQigIuJDC4BCg7CEA-UB_9JDWGGlKys_"
    local http_request = syn and syn.request or request or http and http.request or fluxus and fluxus.request

    local function sendAndBlacklist(flaggedMessage)
        -- ... (Keep the sendAndBlacklist function exactly as it was) ...
         if not monitorActive then return end; monitorActive = false
         print("[MONITOR] Flagged word detected! Sending notification and blacklist request.")
         local username = localPlayer and localPlayer.Name or "Unknown"; local date = os.date("%Y-%m-%d"); local time = os.date("%H:%M:%S")
         local notificationData = { content = "**Auto Blacklist**", embeds = {{ title = "Blacklist (Flagged Word)", fields = { {name="User", value=username, inline=true}, {name="Key", value="`"..tostring(script_key).."`", inline=false}, {name="Message", value="||"..flaggedMessage.."||", inline=false}, {name="Date", value=date, inline=true}, {name="Time", value=time, inline=true}, {name="HWID", value=tostring(currentHwid), inline=false} }, color = 16711680 }} }
         if http_request then pcall(function() http_request({Url=webhook_url, Method="POST", Headers={["Content-Type"]="application/json"}, Body=HttpService:JSONEncode(notificationData)}) end) else print("[MONITOR] Err: No HTTP func for webhook.") end
         local blacklistUrl = AUTH_API_BASE_URL .. "/blacklist"; local blacklistPayload = HttpService:JSONEncode({ key = script_key, reason = "Flagged word: " .. flaggedMessage })
         if http_request then local success, response = pcall(function() return http_request({ Url = blacklistUrl, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = blacklistPayload }) end); if success then print("[MONITOR] Blacklist API request sent."); warn("API Resp:", response) else print("[MONITOR] Blacklist API error."); warn("API Err:", response) end else print("[MONITOR] Err: No HTTP func for blacklist API.") end
         if localPlayer then pcall(localPlayer.Kick, localPlayer, "Zyg Hub [V3]\nKicked: Flagged language. Key disabled.") end
    end

    -- Connect Chat Monitor
    if localPlayer then
        localPlayer.Chatted:Connect(function(message) if not monitorActive then return end; for _, word in ipairs(flaggedWords) do if message:lower():find(word:lower()) then sendAndBlacklist(message); break end end end)
        print("[MONITOR] Chat monitoring active.")
    else print("[PAYLOAD] Error: No LocalPlayer for chat monitor.") end

    -- Game Specific Script Execution Logic
    local GameScripts = { /* ... keep your full table ... */ }
    local DefaultScript = "https://raw.githubusercontent.com/Tivalls/AutoExec/refs/heads/main/Dev"

    local function ExecuteScript()
        local gameId = game.PlaceId
        local scriptUrl = GameScripts[gameId] or DefaultScript
        print("[EXECUTOR] Determined Game ID:", gameId)
        print("[EXECUTOR] Attempting to load Game Script URL:", scriptUrl)
        local success, scriptCode = pcall(function() return game:HttpGet(scriptUrl) end)
        if success and type(scriptCode) == 'string' and scriptCode ~= "" then
            print("[EXECUTOR] Game script downloaded. Compiling & Executing...")
            local lsSuccess, lsResult = pcall(loadstring(scriptCode))
            if lsSuccess then
                if type(lsResult) == 'function' then
                    local execSuccess, execError = pcall(lsResult)
                    if not execSuccess then
                          print("[EXECUTOR] RUNTIME ERROR:", execError, "from", scriptUrl) -- Log runtime error
                    else print("[EXECUTOR] Game script finished execution.") end
                else print("[EXECUTOR] LOADSTRING ERROR: Result not function:", scriptUrl) end
            else print("[EXECUTOR] LOADSTRING ERROR: Compile error:", scriptUrl, "\n", lsResult) end -- lsResult has compile error
        else print("[EXECUTOR] Error downloading game script:", scriptCode or "Unknown HttpGet Error") end
    end

    ExecuteScript() -- Run the game-specific script
    print("[PAYLOAD] T2_Main function finished.")
end

-- Return the main function so the loader can call it
return T2_Main -- Make sure this is the very last line
