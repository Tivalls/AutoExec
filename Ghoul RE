local loaderVerified=_G.ZygHubLoaderVerified;_G.ZygHubLoaderVerified=nil;if not(loaderVerified==true)then warn("Zyg Hub Script Error: Not loaded via authenticated loader. Execution blocked.");return end;local function Script_Main() if not game:IsLoaded()then game.Loaded:Wait() end;if type(Library)=="table" and Library.Unload then Library:Unload() end;local repo='https://raw.githubusercontent.com/violin-suzutsuki/LinoriaLib/main/';local Library=loadstring(game:HttpGet(repo..'Library.lua'))();local ThemeManager=loadstring(game:HttpGet(repo..'addons/ThemeManager.lua'))();local SaveManager=loadstring(game:HttpGet(repo..'addons/SaveManager.lua'))();local hwid=game:GetService("RbxAnalyticsService"):GetClientId();local Window=Library:CreateWindow({Title='Zyg Hub [V3] {'..hwid..'}',Center=true,AutoShow=true,TabPadding=8,MenuFadeTime=0.2});local Tabs={Main=Window:AddTab('Main'),['ESP']=Window:AddTab('ESP'),['Auto Parry']=Window:AddTab('Auto Parry'),['UI Settings']=Window:AddTab('UI Settings')};loadstring(game:HttpGet("https://api.luarmor.net/files/v3/loaders/3341bdf6169ab9afc6d72e68c0634ffa.lua"))();local LeftGroupBox;LeftGroupBox=Tabs["Main"]:AddLeftGroupbox('Main Scripts');local RunService=game:GetService("RunService");local CollectionService=game:GetService("CollectionService");local Players=game:GetService("Players");local Player=Players.LocalPlayer;do local speedHackConnection;local speedHackBv;local speedHackEnabled=false;local speedHackValue=16;local function toggleSpeedHack(toggle) local character=Player and Player.Character;local humanoid=character and character:FindFirstChildOfClass("Humanoid");local rootPart=character and character:FindFirstChild("HumanoidRootPart");if toggle then if not speedHackEnabled and humanoid and rootPart then speedHackBv=Instance.new('BodyVelocity');speedHackBv.MaxForce=Vector3.new(100000,0,100000);speedHackBv.Velocity=Vector3.new(0,0,0);pcall(CollectionService.AddTag,CollectionService,speedHackBv,'AllowedBM');speedHackBv.Parent=rootPart;speedHackConnection=RunService.Heartbeat:Connect(function() local currentHumanoid=character and character:FindFirstChildOfClass("Humanoid");if currentHumanoid and speedHackBv and speedHackBv.Parent then local moveDirection=currentHumanoid.MoveDirection;if moveDirection.Magnitude>0 then speedHackBv.Velocity=moveDirection.Unit*speedHackValue else speedHackBv.Velocity=Vector3.new(0,0,0) end else if speedHackConnection then speedHackConnection:Disconnect();speedHackConnection=nil end;if speedHackBv and speedHackBv.Parent then speedHackBv:Destroy();speedHackBv=nil end;speedHackEnabled=false end end);speedHackEnabled=true end else if speedHackConnection then speedHackConnection:Disconnect();speedHackConnection=nil end;if speedHackBv and speedHackBv.Parent then speedHackBv:Destroy();speedHackBv=nil end;speedHackEnabled=false end end;LeftGroupBox:AddToggle('SpeedHack',{Text='Walkspeed',Default=false,Tooltip='Enables custom walkspeed',Callback=toggleSpeedHack}):AddKeyPicker('SpeedHackKeybind',{Default='',Mode='Toggle',Text='Walkspeed Keybind',NoUI=false});LeftGroupBox:AddSlider('SpeedValue',{Text='Power',Default=16,Min=0,Max=275,Rounding=0,Compact=true,Callback=function(NewValue) speedHackValue=NewValue;if speedHackEnabled then toggleSpeedHack(true)end end});Library:OnUnload(function() toggleSpeedHack(false)end) end;do local jumpPowerEnabled=false;local jumpPowerValue=50;local characterAddedConn_JP,humanoidChangedConn_JP,stateChangedConn_JP;local function applyJumpPower() local character=Player and Player.Character;local humanoid=character and character:FindFirstChildOfClass("Humanoid");if humanoid then humanoid.JumpPower=jumpPowerEnabled and jumpPowerValue or 50;humanoid.UseJumpPower=true end end;local function onCharacterAdded_JP(char) local humanoid=char:WaitForChild("Humanoid");if stateChangedConn_JP and stateChangedConn_JP.Connected then stateChangedConn_JP:Disconnect()end;applyJumpPower();stateChangedConn_JP=humanoid.StateChanged:Connect(applyJumpPower) end;characterAddedConn_JP=Player.CharacterAdded:Connect(onCharacterAdded_JP);if Player.Character then onCharacterAdded_JP(Player.Character)end;LeftGroupBox:AddToggle('JumpPowerToggle',{Text='Jump Power',Default=false,Tooltip='Enables custom jump power',Callback=function(Value) jumpPowerEnabled=Value;applyJumpPower()end}):AddKeyPicker('JumpPowerKeybind',{Default='',Mode='Toggle',Text='Jump Power Keybind',NoUI=false});LeftGroupBox:AddSlider('JumpPowerValue',{Text='Power',Default=50,Min=0,Max=275,Rounding=0,Compact=true,Callback=function(Value) jumpPowerValue=Value;applyJumpPower()end});Library:OnUnload(function() jumpPowerEnabled=false;applyJumpPower();if characterAddedConn_JP then characterAddedConn_JP:Disconnect()end;if stateChangedConn_JP then stateChangedConn_JP:Disconnect()end end) end;LeftGroupBox:AddDivider();do local UIS=game:GetService('UserInputService');local Fly=false;local FlySpeed=200;local Controls={['w']=0,['a']=0,['s']=0,['d']=0,['space']=0,['leftcontrol']=0};local flyRenderStepConn,flyInputBeganConn,flyInputEndedConn,flyCharacterAddedConn;local currentBodyPosition,currentBodyGyro;local function UpdateFly() if not Fly then return end;local character=Player and Player.Character;local hrp=character and character:FindFirstChild("HumanoidRootPart");local humanoid=character and character:FindFirstChildOfClass("Humanoid");local Cam=workspace.CurrentCamera;if hrp and humanoid and Cam then humanoid:ChangeState(Enum.HumanoidStateType.Physics);if not currentBodyPosition or not currentBodyPosition.Parent then currentBodyPosition=Instance.new("BodyPosition",hrp);currentBodyPosition.Name="FlyPosition";currentBodyPosition.MaxForce=Vector3.new(math.huge,math.huge,math.huge);currentBodyPosition.D=50;currentBodyPosition.P=5000 end;if not currentBodyGyro or not currentBodyGyro.Parent then currentBodyGyro=Instance.new("BodyGyro",hrp);currentBodyGyro.Name="FlyGyro";currentBodyGyro.MaxTorque=Vector3.new(math.huge,math.huge,math.huge);currentBodyGyro.D=50;currentBodyGyro.P=3000 end;local verticalMovement=(Controls['space']-Controls['leftcontrol']);local moveVector=Vector3.new(Controls['d']-Controls['a'],0,Controls['s']-Controls['w']);local relativeMoveVector=Cam.CFrame:VectorToWorldSpace(moveVector);currentBodyPosition.Velocity=Vector3.new(relativeMoveVector.X,verticalMovement,relativeMoveVector.Z).Unit*FlySpeed;currentBodyGyro.CFrame=Cam.CFrame else if currentBodyPosition and currentBodyPosition.Parent then currentBodyPosition:Destroy();currentBodyPosition=nil end;if currentBodyGyro and currentBodyGyro.Parent then currentBodyGyro:Destroy();currentBodyGyro=nil end end end;local function ToggleFly(value) Fly=value;local character=Player and Player.Character;local hrp=character and character:FindFirstChild("HumanoidRootPart");local humanoid=character and character:FindFirstChildOfClass("Humanoid");if not Fly then if currentBodyPosition and currentBodyPosition.Parent then currentBodyPosition:Destroy();currentBodyPosition=nil end;if currentBodyGyro and currentBodyGyro.Parent then currentBodyGyro:Destroy();currentBodyGyro=nil end;if flyRenderStepConn and flyRenderStepConn.Connected then flyRenderStepConn:Disconnect();flyRenderStepConn=nil end;if humanoid then humanoid:ChangeState(Enum.HumanoidStateType.Running)end else if not flyRenderStepConn or not flyRenderStepConn.Connected then flyRenderStepConn=RunService.RenderStepped:Connect(UpdateFly)end end end;flyInputBeganConn=UIS.InputBegan:Connect(function(input,gpe) if gpe then return end;local key=input.KeyCode.Name:lower();if Controls[key]~=nil then Controls[key]=1 end end);flyInputEndedConn=UIS.InputEnded:Connect(function(input,gpe) if gpe then return end;local key=input.KeyCode.Name:lower();if Controls[key]~=nil then Controls[key]=0 end end);flyCharacterAddedConn=Player.CharacterAdded:Connect(function(char) if Fly then local hrp=char:WaitForChild("HumanoidRootPart");if currentBodyPosition and not currentBodyPosition.Parent then currentBodyPosition.Parent=hrp end;if currentBodyGyro and not currentBodyGyro.Parent then currentBodyGyro.Parent=hrp end end end);LeftGroupBox:AddToggle('FlyToggle',{Text='Fly',Default=false,Tooltip='Toggle Fly on and Off',Callback=ToggleFly}):AddKeyPicker('FlyKeybind',{Default='',Mode='Toggle',Text='Fly Keybind',NoUI=false});LeftGroupBox:AddSlider('FlySpeedSlider',{Text='Fly Speed',Default=200,Min=0,Max=1000,Rounding=0,Compact=true,Callback=function(Value) FlySpeed=Value end});Library:OnUnload(function() ToggleFly(false);if flyInputBeganConn then flyInputBeganConn:Disconnect()end;if flyInputEndedConn then flyInputEndedConn:Disconnect()end;if flyCharacterAddedConn then flyCharacterAddedConn:Disconnect()end end) end;LeftGroupBox:AddDivider();do local noclipEnabled=false;local noclipSteppedConn;local originalCollideStates={};local function SetCollisionRecursive(obj,canCollide) if obj:IsA("BasePart")then if noclipEnabled and not originalCollideStates[obj]then originalCollideStates[obj]=obj.CanCollide end;obj.CanCollide=canCollide end;for _,child in ipairs(obj:GetChildren())do SetCollisionRecursive(child,canCollide)end end;local function RestoreCollisions() local character=Player and Player.Character;if character then for part,originalState in pairs(originalCollideStates)do if part and part.Parent then part.CanCollide=originalState end end end;originalCollideStates={} end;local function UpdateNoclip() if not noclipEnabled then return end;local character=Player and Player.Character;if character then SetCollisionRecursive(character,false)end end;local function ToggleNoclip(value) noclipEnabled=value;if noclipEnabled then RestoreCollisions();if not noclipSteppedConn or not noclipSteppedConn.Connected then noclipSteppedConn=RunService.Stepped:Connect(UpdateNoclip)end;UpdateNoclip() else if noclipSteppedConn and noclipSteppedConn.Connected then noclipSteppedConn:Disconnect();noclipSteppedConn=nil end;RestoreCollisions()end end;LeftGroupBox:AddToggle('NoClipToggle',{Text='No Clip',Default=false,Tooltip='Allows you to clip through walls',Callback=ToggleNoclip}):AddKeyPicker('NoClipKeybind',{Default='',Mode='Toggle',Text='No Clip Keybind',NoUI=false});Library:OnUnload(function() ToggleNoclip(false)end) end;LeftGroupBox:AddDivider();do local InfiniteJumpEnabled=false;local JumpHeight=100;local infJumpInputBeganConn;local function InfJumpAction(UserInput) if InfiniteJumpEnabled and UserInput.UserInputType==Enum.UserInputType.Keyboard and UserInput.KeyCode==Enum.KeyCode.Space then local character=Player and Player.Character;local humanoid=character and character:FindFirstChildOfClass("Humanoid");if humanoid then local state=humanoid:GetState();if state==Enum.HumanoidStateType.Jumping or state==Enum.HumanoidStateType.Freefall then local hrp=humanoid.Parent:FindFirstChild("HumanoidRootPart");if hrp then hrp.Velocity=Vector3.new(hrp.Velocity.X,JumpHeight,hrp.Velocity.Z)end end end end end;infJumpInputBeganConn=UIS.InputBegan:Connect(InfJumpAction);LeftGroupBox:AddToggle('Inf Jump',{Text='Infinite Jump',Default=false,Tooltip='Allows you to jump infinitely',Callback=function(Value) InfiniteJumpEnabled=Value end}):AddKeyPicker('InfJumpKeybind',{Default='',Mode='Toggle',Text='Infinite Jump Keybind',NoUI=false});LeftGroupBox:AddSlider('InfJumpHeight',{Text='Jump Power',Default=100,Min=0,Max=300,Rounding=0,Compact=true,Callback=function(Value) JumpHeight=Value end});Library:OnUnload(function() if infJumpInputBeganConn then infJumpInputBeganConn:Disconnect()end end) end;LeftGroupBox:AddDivider();do local chatLogActive=false;local chatLogThread=nil;local function StartChatLogger() if chatLogThread then return end;chatLogActive=true;chatLogThread=task.spawn(function() local lines={};local Text_Drawing=Drawing.new("Text");Text_Drawing.Visible=false;local Square_Drawing=Drawing.new("Square");Square_Drawing.Visible=false;local Square2_Drawing=Drawing.new("Square");Square2_Drawing.Visible=false;local UpdateSignal=Instance.new("BindableEvent");local connections={};local maxLines=17;local function IsStringEmpty(String) return type(String)=='string' and(String:match'^%s+$'~=nil or #String==0);end;local function addLine(player,msg) if IsStringEmpty(msg)or not chatLogActive then return end;local firstname=player:GetAttribute('FirstName')or"";local lastname=player:GetAttribute('LastName')or"";local formattedMsg=string.format("%s %s (%s): %s",firstname,lastname,player.Name,msg);table.insert(lines,formattedMsg);if #lines>maxLines then table.remove(lines,1)end;UpdateSignal:Fire() end;local function setupPlayer(player) if connections[player]then connections[player]:Disconnect()end;connections[player]=player.Chatted:Connect(function(msg) addLine(player,msg)end) end;for _,player in ipairs(Players:GetPlayers())do setupPlayer(player)end;connections.PlayerAdded=Players.PlayerAdded:Connect(setupPlayer);connections.PlayerRemoving=Players.PlayerRemoving:Connect(function(player) if connections[player]then connections[player]:Disconnect();connections[player]=nil;end end);while chatLogActive do local Buffer=table.concat(lines,"\n");local VPSize=workspace.CurrentCamera and workspace.CurrentCamera.ViewportSize or Vector2.new(800,600);Square_Drawing.Size=Vector2.new(math.min(500,VPSize.X-50),math.min(300,VPSize.Y-50));Square2_Drawing.Size=Square_Drawing.Size+Vector2.new(4,4);Square_Drawing.Position=Vector2.new(VPSize.X-Square_Drawing.Size.X-15,VPSize.Y-Square_Drawing.Size.Y-15);Square2_Drawing.Position=Square_Drawing.Position-Vector2.new(2,2);Square2_Drawing.Transparency=1;Square2_Drawing.Visible=true;Square2_Drawing.Filled=false;Square2_Drawing.Thickness=1;Square2_Drawing.Color=Color3.new(0.8,0.8,0.8);Square2_Drawing.ZIndex=1;Square_Drawing.Transparency=0.6;Square_Drawing.Visible=true;Square_Drawing.Filled=true;Square_Drawing.Color=Color3.new(0.1,0.1,0.1);Square_Drawing.ZIndex=0;Text_Drawing.Position=Square_Drawing.Position+Vector2.new(5,5);Text_Drawing.Size=14;Text_Drawing.Text=Buffer;Text_Drawing.Outline=true;Text_Drawing.Color=Color3.new(1,1,1);Text_Drawing.OutlineColor=Color3.new(0,0,0);Text_Drawing.Transparency=1;Text_Drawing.Visible=true;Text_Drawing.ZIndex=2;Text_Drawing.Font=2;local _,timedOut=UpdateSignal.Event:Wait(0.5);if not chatLogActive then break end end;Text_Drawing:Remove();Square_Drawing:Remove();Square2_Drawing:Remove();UpdateSignal:Destroy();for _,conn in pairs(connections)do if conn and conn.Connected then conn:Disconnect()end end;chatLogThread=nil;chatLogActive=false end) end;local function StopChatLogger() chatLogActive=false end;LeftGroupBox:AddToggle('ChatLogger',{Text='Chat Logger',Default=false,Tooltip='Shows chat history overlay',Callback=function(Value) if Value then StartChatLogger()else StopChatLogger()end end});Library:OnUnload(StopChatLogger) end;LeftGroupBox:AddDivider();do local cloneref=cloneref or function(o) return o end;local TextChatService=cloneref(game:GetService("TextChatService"));local ReplicatedStorage=cloneref(game:GetService("ReplicatedStorage"));local LocalPlayer=Players.LocalPlayer;local FakeUserId="Zyg Hub User";local streamerModeEnabled=false;local connections_SM={};local function UpdateObjectUserId(Object,Property) if not Object or not Object[Property]or type(Object[Property])~="string" then return end;local originalValue=Object[Property];local newValue;if streamerModeEnabled then newValue=originalValue:gsub(tostring(LocalPlayer.UserId),FakeUserId) else newValue=originalValue:gsub(FakeUserId,tostring(LocalPlayer.UserId)) end;if newValue~=originalValue then Object[Property]=newValue end end;local function NewObject(Object) pcall(function() if Object:IsA("TextLabel")or Object:IsA("TextButton")or Object:IsA("TextBox")then UpdateObjectUserId(Object,"Text");local conn=Object:GetPropertyChangedSignal("Text"):Connect(function() UpdateObjectUserId(Object,"Text")end);table.insert(connections_SM,conn) elseif Object:IsA("ImageLabel")or Object:IsA("ImageButton")then UpdateObjectUserId(Object,"Image");local conn=Object:GetPropertyChangedSignal("Image"):Connect(function() UpdateObjectUserId(Object,"Image")end);table.insert(connections_SM,conn) end end) end;local function UpdateAllObjects() for _,Object in pairs(game:GetDescendants())do NewObject(Object)end end;local function ToggleStreamerMode(value) streamerModeEnabled=value;UpdateAllObjects() end;UpdateAllObjects();local descAddedConn=game.DescendantAdded:Connect(NewObject);table.insert(connections_SM,descAddedConn);if TextChatService.ChatVersion==Enum.ChatVersion.TextChatService then local msgReceivedConn=TextChatService.MessageReceived:Connect(function(MessageData) NewObject(MessageData)end);table.insert(connections_SM,msgReceivedConn) else local chatEvent=ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")and ReplicatedStorage.DefaultChatSystemChatEvents:FindFirstChild("OnMessageDoneFiltering");if chatEvent then local legacyChatConn=chatEvent.OnClientEvent:Connect(function(MessageData) NewObject(MessageData)end);table.insert(connections_SM,legacyChatConn)end end;LeftGroupBox:AddToggle('StreamerModeToggle',{Text="Streamer Mode",Default=false,Tooltip="Replaces your UserID in UI elements.",Callback=ToggleStreamerMode});Library:OnUnload(function() streamerModeEnabled=false;UpdateAllObjects();for _,conn in ipairs(connections_SM)do if conn and conn.Connected then conn:Disconnect()end end;connections_SM={} end) end;LeftGroupBox:AddDivider();LeftGroupBox:AddSlider('Grapics Slider',{Text='Graphics Level',Default=UserSettings().GameSettings.SavedQualityLevel.Value,Min=1,Max=10,Rounding=0,Compact=false,Callback=function(Value) settings().Rendering.QualityLevel=Enum.QualityLevel[("Level%.2d"):format(Value)]end});LeftGroupBox=Tabs["Main"]:AddLeftGroupbox('Visuals');do local fullBrightEnabled=false;local normalLightingSettings={};local isInitialized_FB=false;local connections_FB={};local function StoreNormalSettings() normalLightingSettings.Brightness=game.Lighting.Brightness;normalLightingSettings.ClockTime=game.Lighting.ClockTime;normalLightingSettings.FogEnd=game.Lighting.FogEnd;normalLightingSettings.GlobalShadows=game.Lighting.GlobalShadows;normalLightingSettings.Ambient=game.Lighting.Ambient;normalLightingSettings.OutdoorAmbient=game.Lighting.OutdoorAmbient;isInitialized_FB=true end;local function ApplyFullbright() if not isInitialized_FB then StoreNormalSettings()end;game.Lighting.Brightness=1;game.Lighting.ClockTime=12;game.Lighting.FogEnd=100000;game.Lighting.GlobalShadows=false;game.Lighting.Ambient=Color3.fromRGB(128,128,128);game.Lighting.OutdoorAmbient=Color3.fromRGB(128,128,128) end;local function RestoreNormalLighting() if not isInitialized_FB then return end;pcall(function() game.Lighting.Brightness=normalLightingSettings.Brightness;game.Lighting.ClockTime=normalLightingSettings.ClockTime;game.Lighting.FogEnd=normalLightingSettings.FogEnd;game.Lighting.GlobalShadows=normalLightingSettings.GlobalShadows;game.Lighting.Ambient=normalLightingSettings.Ambient;game.Lighting.OutdoorAmbient=normalLightingSettings.OutdoorAmbient end) end;local function ToggleFullbright(value) fullBrightEnabled=value;if fullBrightEnabled then ApplyFullbright() else RestoreNormalLighting()end end;local function setupListeners_FB() local function CreateListener(propName,brightValue,normalTable) connections_FB[propName]=game.Lighting:GetPropertyChangedSignal(propName):Connect(function() if fullBrightEnabled and game.Lighting[propName]~=brightValue then task.defer(ApplyFullbright) elseif not fullBrightEnabled then normalTable[propName]=game.Lighting[propName]end end) end;CreateListener("Brightness",1,normalLightingSettings);CreateListener("ClockTime",12,normalLightingSettings);CreateListener("FogEnd",100000,normalLightingSettings);CreateListener("GlobalShadows",false,normalLightingSettings);CreateListener("Ambient",Color3.fromRGB(128,128,128),normalLightingSettings);CreateListener("OutdoorAmbient",Color3.fromRGB(128,128,128),normalLightingSettings) end;setupListeners_FB();LeftGroupBox:AddToggle('FullBright',{Text='Fullbright',Default=false,Tooltip="Maximizes brightness, removes shadows/fog",Callback=ToggleFullbright});Library:OnUnload(function() ToggleFullbright(false);for _,conn in pairs(connections_FB)do if conn and conn.Connected then conn:Disconnect()end end;connections_FB={} end) end;do local noFogEnabled=false;local normalFogEnd=game.Lighting.FogEnd;local normalAtmosphereDensity={};local fogConn,atmosAddedConn,atmosDensityConns=nil,nil,{};local function UpdateFogSettings() pcall(function() game.Lighting.FogEnd=noFogEnabled and 100000 or normalFogEnd;for atmos,normalDensity in pairs(normalAtmosphereDensity)do if atmos and atmos.Parent then atmos.Density=noFogEnabled and 0 or normalDensity end end end) end;local function SetupAtmosphere(atmos) if atmos:IsA("Atmosphere")then local normalDensity=atmos.Density;normalAtmosphereDensity[atmos]=normalDensity;atmosDensityConns[atmos]=atmos:GetPropertyChangedSignal("Density"):Connect(function() if noFogEnabled and atmos.Density~=0 then atmos.Density=0 elseif not noFogEnabled then normalAtmosphereDensity[atmos]=atmos.Density end end);if noFogEnabled then atmos.Density=0 end end end;local function ToggleNoFog(value) noFogEnabled=value;if noFogEnabled then normalFogEnd=game.Lighting.FogEnd;if not fogConn or not fogConn.Connected then fogConn=game.Lighting:GetPropertyChangedSignal("FogEnd"):Connect(function() if noFogEnabled and game.Lighting.FogEnd~=100000 then game.Lighting.FogEnd=100000 elseif not noFogEnabled then normalFogEnd=game.Lighting.FogEnd end end)end;if not atmosAddedConn or not atmosAddedConn.Connected then atmosAddedConn=game.Lighting.DescendantAdded:Connect(SetupAtmosphere)end;normalAtmosphereDensity={};atmosDensityConns={};for _,child in ipairs(game.Lighting:GetDescendants())do SetupAtmosphere(child)end;UpdateFogSettings() else if fogConn then fogConn:Disconnect();fogConn=nil end;if atmosAddedConn then atmosAddedConn:Disconnect();atmosAddedConn=nil end;for _,conn in pairs(atmosDensityConns)do if conn and conn.Connected then conn:Disconnect()end end;atmosDensityConns={};UpdateFogSettings()end end;LeftGroupBox:AddToggle('No Fog',{Text='No Fog',Default=false,Tooltip="Removes distance fog",Callback=ToggleNoFog});Library:OnUnload(function() ToggleNoFog(false)end) end;LeftGroupBox=Tabs.Main:AddLeftGroupbox('Event Farm');do local TweenService=game:GetService("TweenService");local EventFarmEnabled=false;local TweenSpeed=150;local TweenMode="Straight";local MaxDistance=15;local eventFarmLoopThread=nil;local ActiveTween=nil;local function GetClosestStreet() local Closest,ShortestDist=nil,math.huge;local hrp=Player and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart");if not hrp then return nil end;local debrisFolder=workspace:FindFirstChild("DebrisFolder");if not debrisFolder then return nil end;for _,v in pairs(debrisFolder:GetChildren())do if v:IsA("Model")and(v.Name=="Street 1"or v.Name=="Street 2")then local primaryPart=v.PrimaryPart or v:FindFirstChildWhichIsA("BasePart");if primaryPart then local Pos=primaryPart.Position;local Dist=(hrp.Position-Pos).Magnitude;if Dist<ShortestDist then Closest=primaryPart;ShortestDist=Dist end end end end;return Closest end;local function TweenTo(targetPart) if ActiveTween then ActiveTween:Cancel();ActiveTween=nil end;local hrp=Player and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart");if not hrp or not targetPart then return end;local targetPos=targetPart.Position;local YOffset=0;if TweenMode=="Above"then YOffset=50 elseif TweenMode=="Below"then YOffset=-50 end;local goal={Position=Vector3.new(targetPos.X,targetPos.Y+YOffset,targetPos.Z)};local dist=(hrp.Position-goal.Position).Magnitude;local duration=dist/math.max(TweenSpeed,10);local info=TweenInfo.new(duration,Enum.EasingStyle.Linear);hrp.Anchored=false;ActiveTween=TweenService:Create(hrp,info,goal);ActiveTween:Play();ActiveTween.Completed:Connect(function(state) if state==Enum.TweenStatus.Completed then end;ActiveTween=nil end) end;local function EventFarmLoop() local ReachedStreetPart=nil;while EventFarmEnabled do local hrp=Player and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart");if not hrp then task.wait(1);continue end;if ReachedStreetPart and not ReachedStreetPart.Parent then ReachedStreetPart=nil;if ActiveTween then ActiveTween:Cancel()end end;if not ReachedStreetPart then local closestStreetPart=GetClosestStreet();if closestStreetPart then if not ActiveTween and(hrp.Position-closestStreetPart.Position).Magnitude>MaxDistance+5 then TweenTo(closestStreetPart) elseif(hrp.Position-closestStreetPart.Position).Magnitude<=MaxDistance then ReachedStreetPart=closestStreetPart;if ActiveTween then ActiveTween:Cancel()end end else if ActiveTween then ActiveTween:Cancel()end end elseif(hrp.Position-ReachedStreetPart.Position).Magnitude>MaxDistance and not ActiveTween then TweenTo(ReachedStreetPart)end;task.wait(0.5)end;if ActiveTween then ActiveTween:Cancel();ActiveTween=nil end;eventFarmLoopThread=nil end;local function ToggleEventFarm(value) EventFarmEnabled=value;if EventFarmEnabled then if not eventFarmLoopThread then eventFarmLoopThread=task.spawn(EventFarmLoop)end else if ActiveTween then ActiveTween:Cancel();ActiveTween=nil end end end;LeftGroupBox:AddToggle('EventTP',{Text='Autofarm Events',Default=false,Tooltip='Tweens you to the nearest Event',Callback=ToggleEventFarm});LeftGroupBox:AddSlider('TweenSpeed',{Text='Speed',Default=150,Min=10,Max=500,Rounding=0,Compact=true,Callback=function(value) TweenSpeed=value end});LeftGroupBox:AddDropdown('TweenSelect',{Values={'Above','Below','Straight'},Default=3,Multi=false,Text='Tween Location',Tooltip='Select the vertical position for tweening',Callback=function(Value) TweenMode=Value end});Library:OnUnload(function() ToggleEventFarm(false)end) end;LeftGroupBox=Tabs.Main:AddLeftGroupbox('Mob Farm');do local mobFarmEnabled=false;local selectedMobName="None";local distanceBehind=5;local mobDropdown=nil;local updateDropdownThread=nil;local teleportLoopThread=nil;local function UpdateMobDropdown() while mobDropdown do local mobList={};local hrp=Player and Player.Character and Player.Character:FindFirstChild("HumanoidRootPart");if hrp and workspace:FindFirstChild("Entities")then for _,obj in ipairs(workspace.Entities:GetChildren())do if obj:IsA("Model")and obj:FindFirstChild("HumanoidRootPart")and obj:FindFirstChildOfClass("Humanoid")and not Players:GetPlayerFromCharacter(obj)then local distance=(obj.HumanoidRootPart.Position-hrp.Position).Magnitude;table.insert(mobList,{Name=obj.Name,Distance=distance,Instance=obj})end end end;table.sort(mobList,function(a,b) return a.Distance<b.Distance end);local sortedNames={"None"};for _,mobData in ipairs(mobList)do table.insert(sortedNames,mobData.Name)end;local currentSelection=mobDropdown.Value or"None";local currentSelectionIndex=mobDropdown.Index;mobDropdown:SetValues(sortedNames);local foundIndex=table.find(sortedNames,currentSelection);if foundIndex then mobDropdown:SetValue(foundIndex) elseif #sortedNames>=currentSelectionIndex and currentSelectionIndex>0 then mobDropdown:SetValue(currentSelectionIndex) else mobDropdown:SetValue(1)end;selectedMobName=mobDropdown.Value;task.wait(1)end;updateDropdownThread=nil end;local function TeleportLoop() while mobFarmEnabled do if selectedMobName~="None"then local targetMob=workspace.Entities:FindFirstChild(selectedMobName);local targetHRP=targetMob and targetMob:FindFirstChild("HumanoidRootPart");local playerChar=Player and Player.Character;local playerHRP=playerChar and playerChar:FindFirstChild("HumanoidRootPart");if targetHRP and playerHRP then local behindPosition=targetHRP.CFrame*CFrame.new(0,0,distanceBehind);playerHRP.CFrame=behindPosition end end;task.wait(0.1)end;teleportLoopThread=nil end;local function ToggleMobFarm(value) mobFarmEnabled=value;if mobFarmEnabled then if not teleportLoopThread then teleportLoopThread=task.spawn(TeleportLoop)end end end;mobDropdown=LeftGroupBox:AddDropdown('MyDropdown',{Values={'None'},Default=1,Multi=false,Text='Select a Mob',Tooltip='Choose a mob to teleport to',Callback=function(Value) selectedMobName=Value end});LeftGroupBox:AddToggle('MobFarm',{Text='Enable Mob TP',Default=false,Tooltip='Enable teleporting to selected mob',Callback=ToggleMobFarm});LeftGroupBox:AddSlider('MySlider',{Text='Distance Behind',Default=5,Min=0,Max=25,Rounding=0,Compact=false,Callback=function(Value) distanceBehind=Value end});updateDropdownThread=task.spawn(UpdateMobDropdown);Library:OnUnload(function() ToggleMobFarm(false);mobDropdown=nil end) end;LeftGroupBox:AddDivider();do local VoidMobsEnabled=false;local KillRadius=50;local voidMobsLoopThread=nil;local function VoidMobsLoop() while VoidMobsEnabled do local playerChar=Player and Player.Character;local myRoot=playerChar and playerChar:FindFirstChild("HumanoidRootPart");if not myRoot then task.wait(0.5);continue end;if workspace:FindFirstChild("Entities")then for _,mob in pairs(workspace.Entities:GetChildren())do if mob:IsA("Model")and mob~=playerChar then local humanoid=mob:FindFirstChildOfClass("Humanoid");local rootPart=mob:FindFirstChild("HumanoidRootPart");if humanoid and rootPart and humanoid.Health>0 then local distance=(myRoot.Position-rootPart.Position).Magnitude;if distance<=KillRadius then humanoid:TakeDamage(humanoid.MaxHealth*2)end end end end end;task.wait(0.2)end;voidMobsLoopThread=nil end;local function ToggleVoidMobs(value) VoidMobsEnabled=value;if VoidMobsEnabled then if not voidMobsLoopThread then voidMobsLoopThread=task.spawn(VoidMobsLoop)end end end;LeftGroupBox:AddToggle('VMToggle',{Text='Void Mobs',Default=false,Tooltip="Insta-kills mobs within radius",Callback=ToggleVoidMobs}):AddKeyPicker('VMKeybind',{Default='',Mode='Toggle',Text='Void Mobs Keybind',NoUI=false});LeftGroupBox:AddSlider('VoidRadiusSlider',{Text='Void Radius',Default=50,Min=0,Max=200,Rounding=0,Callback=function(Value) KillRadius=Value end});Library:OnUnload(function() ToggleVoidMobs(false)end) end;local RightGroupBox;RightGroupBox=Tabs["Main"]:AddRightGroupbox('Integrities');do local antiAFKEnabled=false;local antiAFKConnection=nil;local function AntiAFKIdle() if not antiAFKEnabled then return end;pcall(function() local vu=game:GetService("VirtualUser");vu:Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame);task.wait(0.1);vu:Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame)end) end;local function ToggleAntiAFK(value) antiAFKEnabled=value;if antiAFKEnabled then if not antiAFKConnection or not antiAFKConnection.Connected then antiAFKConnection=Player.Idled:Connect(AntiAFKIdle)end else if antiAFKConnection and antiAFKConnection.Connected then antiAFKConnection:Disconnect();antiAFKConnection=nil end end end;RightGroupBox:AddToggle('AntiAFKToggle',{Text='Anti AFK',Default=false,Tooltip="Prevents idle kick",Callback=ToggleAntiAFK});Library:OnUnload(function() ToggleAntiAFK(false)end) end;do local proximityAlertEnabled=false;local proximityUpdateConn=nil;local proximityGui=nil;local usernameLabel,distanceLabel;local function CreateProximityGUI() if proximityGui then return end;proximityGui=Instance.new("ScreenGui",Player.PlayerGui);proximityGui.Name="ProximityAlert";proximityGui.ResetOnSpawn=false;proximityGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling;usernameLabel=Instance.new("TextLabel",proximityGui);usernameLabel.Size=UDim2.new(0,200,0,25);usernameLabel.Position=UDim2.new(0.5,-100,0,10);usernameLabel.BackgroundTransparency=0.5;usernameLabel.BackgroundColor3=Color3.new(0,0,0);usernameLabel.TextColor3=Color3.new(1,0,0);usernameLabel.Font=Enum.Font.SourceSansBold;usernameLabel.TextSize=16;usernameLabel.Text="Player Nearby:";usernameLabel.Visible=false;distanceLabel=Instance.new("TextLabel",proximityGui);distanceLabel.Size=UDim2.new(0,200,0,20);distanceLabel.Position=UDim2.new(0.5,-100,0,35);distanceLabel.BackgroundTransparency=0.5;distanceLabel.BackgroundColor3=Color3.new(0,0,0);distanceLabel.TextColor3=Color3.new(1,1,1);distanceLabel.Font=Enum.Font.SourceSans;distanceLabel.TextSize=14;distanceLabel.Text="Distance: 0 studs";distanceLabel.Visible=false end;local function DestroyProximityGUI() if proximityGui then proximityGui:Destroy();proximityGui=nil;usernameLabel=nil;distanceLabel=nil;end end;local function UpdateProximity() if not proximityAlertEnabled or not proximityGui then return end;local closestPlayer,closestDistance=nil,500;local playerChar=Player and Player.Character;local humanoidRootPart=playerChar and playerChar:FindFirstChild("HumanoidRootPart");if not humanoidRootPart then usernameLabel.Visible=false;distanceLabel.Visible=false;return end;for _,otherPlayer in ipairs(Players:GetPlayers())do if otherPlayer~=Player and otherPlayer.Character and otherPlayer.Character:FindFirstChild("HumanoidRootPart")then local otherRootPart=otherPlayer.Character.HumanoidRootPart;local distance=(humanoidRootPart.Position-otherRootPart.Position).Magnitude;if distance<=closestDistance then closestPlayer=otherPlayer;closestDistance=distance end end end;if closestPlayer then usernameLabel.Text="Player Nearby: "..closestPlayer.Name;distanceLabel.Text="Distance: "..math.floor(closestDistance).." studs";usernameLabel.Visible=true;distanceLabel.Visible=true else usernameLabel.Visible=false;distanceLabel.Visible=false end end;local function ToggleProximityAlert(state) proximityAlertEnabled=state;if proximityAlertEnabled then CreateProximityGUI();if not proximityUpdateConn or not proximityUpdateConn.Connected then proximityUpdateConn=RunService.RenderStepped:Connect(UpdateProximity)end else DestroyProximityGUI();if proximityUpdateConn and proximityUpdateConn.Connected then proximityUpdateConn:Disconnect();proximityUpdateConn=nil end end end;RightGroupBox:AddToggle('ProximityToggle',{Text='Proximity Alert',Default=false,Tooltip='Shows a GUI if a player is within 500 studs',Callback=ToggleProximityAlert});Library:OnUnload(function() ToggleProximityAlert(false)end) end;RightGroupBox=Tabs["Main"]:AddRightGroupbox('Humanoid');do local anchorEnabled=false;local function ToggleAnchor(value) anchorEnabled=value;local character=Player and Player.Character;local hrp=character and character:FindFirstChild("HumanoidRootPart");if hrp then hrp.Anchored=anchorEnabled end end;RightGroupBox:AddToggle('Anchor Body/Torso',{Text='Anchor Body',Default=false,Tooltip="Anchors your HumanoidRootPart",Callback=ToggleAnchor}):AddKeyPicker('AnchorKeybind',{Default='',Mode='Toggle',Text='Anchor Body Keybind',NoUI=false});Library:OnUnload(function() ToggleAnchor(false)end) end;RightGroupBox=Tabs.Main:AddRightGroupbox('Missions');do local MissionFarm=false;local MissionDifficulty="Hard";local MissionFallback=false;local MissionAutoGrabThread=nil;local MissionAutofarmThread=nil;local MissionTweenActive=false;local MissionVoidMobsRequired=true;local MissionTweenSpeed=150;local function AutoGrabMissionLoop() local priorities={Easy={"Medium","Hard"},Medium={"Easy","Hard"},Hard={"Medium","Easy"}};while MissionFarm do local missionBoard=workspace:FindFirstChild("MissionBoards")and workspace.MissionBoards:FindFirstChild("CCG")and workspace.MissionBoards.CCG:FindFirstChild("MissionBoard");local holder=missionBoard and missionBoard:FindFirstChild("Holder");if holder then local found=false;for i=1,10 do local mission=holder:FindFirstChild(tostring(i));local surface=mission and mission:FindFirstChild("SurfaceGui");local rating=surface and surface:FindFirstChild("Rating");if rating and rating:IsA("TextLabel")and rating.Text==MissionDifficulty and mission:FindFirstChild("ClickDetector")then fireclickdetector(mission.ClickDetector);found=true;break end end;if not found and MissionFallback then for _,diff in ipairs(priorities[MissionDifficulty])do for i=1,10 do local mission=holder:FindFirstChild(tostring(i));local surface=mission and mission:FindFirstChild("SurfaceGui");local rating=surface and surface:FindFirstChild("Rating");if rating and rating:IsA("TextLabel")and rating.Text==diff and mission:FindFirstChild("ClickDetector")then fireclickdetector(mission.ClickDetector);found=true;break end end;if found then break end end end end;task.wait(1.5)end;MissionAutoGrabThread=nil end;local function AutofarmMissionLoop() while MissionTweenActive do local player=Players.LocalPlayer;local playerEntity=workspace.Entities:FindFirstChild(player.Name);local hrp=player.Character and player.Character:FindFirstChild("HumanoidRootPart");local icon=playerEntity and playerEntity:FindFirstChild("MissionIcon");if icon and hrp then local goal=icon.Position+Vector3.new(0,5,0);local distance=(hrp.Position-goal).Magnitude;local tweenInfo=TweenInfo.new(distance/math.max(MissionTweenSpeed,10),Enum.EasingStyle.Linear);hrp.Anchored=false;local tween=TweenService:Create(hrp,tweenInfo,{Position=goal});tween:Play();local completed=false;local conn=tween.Completed:Connect(function() completed=true end);while not completed and icon and icon.Parent do task.wait(0.1);if MissionVoidMobsRequired and not Toggles.VMToggle.Value then tween:Cancel();Library:Notify("Autofarm Mission: Void Mobs disabled, stopping.",3);MissionTweenActive=false;break end end;conn:Disconnect();if not MissionTweenActive then break end;if icon and icon.Parent then hrp.Anchored=true;Library:Notify("Autofarm Mission: Arrived, waiting for mobs.",2);while icon and icon.Parent and MissionTweenActive do task.wait(0.2);if MissionVoidMobsRequired and not Toggles.VMToggle.Value then Library:Notify("Autofarm Mission: Void Mobs disabled while waiting, stopping.",3);MissionTweenActive=false;break end end;hrp.Anchored=false;if MissionTweenActive then Library:Notify("Autofarm Mission: Completed/Icon Gone.",2)end end else task.wait(1)end;task.wait(0.2)end;MissionAutofarmThread=nil;if Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")then Player.Character.HumanoidRootPart.Anchored=false end end;local function ToggleAutoGrab(value) MissionFarm=value;if MissionFarm then if not MissionAutoGrabThread then MissionAutoGrabThread=task.spawn(AutoGrabMissionLoop)end end end;local function ToggleAutofarm(value) if value and MissionVoidMobsRequired and not Toggles.VMToggle.Value then Library:Notify("Cannot start Autofarm Mission: Void Mobs must be enabled.",5,"Error");Toggles.MissionFarmtoggle:SetValue(false);return end;MissionTweenActive=value;if MissionTweenActive then if not MissionAutofarmThread then MissionAutofarmThread=task.spawn(AutofarmMissionLoop)end end end;RightGroupBox:AddToggle('AutoGetMission',{Text='Auto Grab Missions',Default=false,Tooltip='Automatically grabs the chosen mission.',Callback=ToggleAutoGrab});RightGroupBox:AddDropdown('MissionSelect',{Values={'Easy','Medium','Hard'},Default=3,Multi=false,Text='Mission Select',Tooltip='Select preferred mission difficulty',Callback=function(Value) MissionDifficulty=Value end});RightGroupBox:AddDivider();RightGroupBox:AddToggle('MissionFallbackToggle',{Text='Enable Difficulty Fallback',Default=false,Tooltip='Tries other difficulties if selected one is missing',Callback=function(Value) MissionFallback=Value end});RightGroupBox:AddDivider();RightGroupBox:AddToggle('MissionFarmtoggle',{Text='Autofarm Missions',Default=false,Tooltip='Tweens to your current mission target [Requires Void Mobs Enabled]',Callback=ToggleAutofarm});RightGroupBox:AddSlider('MissionFarmTweenSpeed',{Text='Speed',Default=150,Min=10,Max=500,Rounding=0,Compact=true,Callback=function(value) MissionTweenSpeed=value end});Library:OnUnload(function() ToggleAutoGrab(false);ToggleAutofarm(false)end) end;LeftGroupBox=Tabs["ESP"]:AddLeftGroupbox('Player ESP');do local playerESPEnabled=false;local espUpdateConn=nil;local playerAddedConn_PESP=nil;local playerRemovingConn_PESP=nil;local characterAddedConns_PESP={};local function updateSinglePlayerESP(player) if not playerESPEnabled or player==Player or not player.Character or not Player.Character then return end;local localHead=Player.Character:FindFirstChild("Head");local targetHead=player.Character:FindFirstChild("Head");local humanoid=player.Character:FindFirstChildOfClass("Humanoid");if not localHead or not targetHead or not humanoid then return end;local distance=math.floor((localHead.Position-targetHead.Position).Magnitude);local currentHealth=math.floor(humanoid.Health);local maxHealth=math.floor(humanoid.MaxHealth);local text=string.format("%s\nDist: %d\nHP: %d/%d",player.Name,distance,currentHealth,maxHealth);local billboardGui=targetHead:FindFirstChild("ZygHubPlayerESP");if not billboardGui then billboardGui=Instance.new("BillboardGui",targetHead);billboardGui.Name="ZygHubPlayerESP";billboardGui.Adornee=targetHead;billboardGui.Size=UDim2.new(0,120,0,60);billboardGui.StudsOffset=Vector3.new(0,2.5,0);billboardGui.AlwaysOnTop=true;billboardGui.LightInfluence=0;billboardGui.ResetOnSpawn=false;billboardGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling;local textLabel=Instance.new("TextLabel",billboardGui);textLabel.Name="InfoLabel";textLabel.Size=UDim2.new(1,0,1,0);textLabel.BackgroundTransparency=1;textLabel.TextColor3=Color3.new(1,0,0);textLabel.Font=Enum.Font.SourceSansBold;textLabel.TextScaled=true;textLabel.TextStrokeColor3=Color3.new(0,0,0);textLabel.TextStrokeTransparency=0;textLabel.Visible=true end;billboardGui.InfoLabel.Text=text;billboardGui.Enabled=humanoid.Health>0 end;local function removeSinglePlayerESP(player) if player and player.Character then local head=player.Character:FindFirstChild("Head");local gui=head and head:FindFirstChild("ZygHubPlayerESP");if gui then gui:Destroy()end end end;local function updateAllPlayerESP() if not playerESPEnabled then return end;for _,player in ipairs(Players:GetPlayers())do updateSinglePlayerESP(player)end end;local function cleanupPlayerESP(player) removeSinglePlayerESP(player);if characterAddedConns_PESP[player]then characterAddedConns_PESP[player]:Disconnect();characterAddedConns_PESP[player]=nil end end;local function setupPlayerESP(player) cleanupPlayerESP(player);characterAddedConns_PESP[player]=player.CharacterAdded:Connect(function(char) local head=char:WaitForChild("Head");local humanoid=char:WaitForChild("Humanoid");task.wait(0.1);updateSinglePlayerESP(player);local healthChangedConn=humanoid.HealthChanged:Connect(function() updateSinglePlayerESP(player)end);char.AncestryChanged:Connect(function(_,parent) if not parent then healthChangedConn:Disconnect()end end) end);if player.Character then updateSinglePlayerESP(player);local humanoid=player.Character:FindFirstChildOfClass("Humanoid");if humanoid then local healthChangedConn=humanoid.HealthChanged:Connect(function() updateSinglePlayerESP(player)end);player.Character.AncestryChanged:Connect(function(_,parent) if not parent then healthChangedConn:Disconnect()end end)end end end;local function TogglePlayerESP(value) playerESPEnabled=value;if playerESPEnabled then if not espUpdateConn or not espUpdateConn.Connected then espUpdateConn=RunService.Heartbeat:Connect(updateAllPlayerESP)end;if not playerAddedConn_PESP or not playerAddedConn_PESP.Connected then playerAddedConn_PESP=Players.PlayerAdded:Connect(setupPlayerESP)end;if not playerRemovingConn_PESP or not playerRemovingConn_PESP.Connected then playerRemovingConn_PESP=Players.PlayerRemoving:Connect(cleanupPlayerESP)end;for _,p in ipairs(Players:GetPlayers())do setupPlayerESP(p)end else if espUpdateConn then espUpdateConn:Disconnect();espUpdateConn=nil end;if playerAddedConn_PESP then playerAddedConn_PESP:Disconnect();playerAddedConn_PESP=nil end;if playerRemovingConn_PESP then playerRemovingConn_PESP:Disconnect();playerRemovingConn_PESP=nil end;for _,p in ipairs(Players:GetPlayers())do cleanupPlayerESP(p)end;characterAddedConns_PESP={}end end;LeftGroupBox:AddToggle('Player ESP',{Text='Players',Default=false,Tooltip="Show info above players",Callback=TogglePlayerESP});Library:OnUnload(function() TogglePlayerESP(false)end) end;do local mobEspEnabled=false;local mobESPUpdateConn=nil;local mobLabels={};local mobEvents={};local function synTextLabel() local TextLabel=Drawing.new("Text");TextLabel.Size=14;TextLabel.Center=true;TextLabel.Outline=true;TextLabel.Font=2;TextLabel.OutlineColor=Color3.new(0,0,0);TextLabel.Color=Color3.new(1,1,1);TextLabel.Visible=false;return TextLabel end;local function worldpoint_to_viewpoint(pos) local cam=workspace.CurrentCamera;if not cam then return Vector2.new(),false end;local screenPos,onScreen=cam:WorldToViewportPoint(pos);return Vector2.new(screenPos.X,screenPos.Y),onScreen end;local function cleanupMobLabels(mob) if mobLabels[mob]then if mobLabels[mob].NameTag then mobLabels[mob].NameTag:Remove()end;if mobLabels[mob].InfoTag then mobLabels[mob].InfoTag:Remove()end;mobLabels[mob]=nil end end;local function addMobToRenderList(mob) if mob:IsA("Model")and mob:FindFirstChild("Head")and mob:FindFirstChildOfClass("Humanoid")and not Players:GetPlayerFromCharacter(mob)then if not mobLabels[mob]then mobLabels[mob]={NameTag=synTextLabel(),InfoTag=synTextLabel()}end;local ancestryConn=mob.AncestryChanged:Connect(function(_,parent) if not parent then cleanupMobLabels(mob);mobEvents[mob]:Disconnect();mobEvents[mob]=nil end end);mobEvents[mob]=ancestryConn end end;local function updateMobESPDrawing() if not mobEspEnabled then return end;local localChar=Player and Player.Character;local localHeadPos=localChar and localChar:FindFirstChild("Head")and localChar.Head.Position;for mob,labels in pairs(mobLabels)do if not mob or not mob.Parent or not mob:FindFirstChild("Head")or not mob:FindFirstChildOfClass("Humanoid")then cleanupMobLabels(mob);continue end;local head=mob.Head;local humanoid=mob:FindFirstChildOfClass("Humanoid");local headPos=head.Position;local screenPos,onScreen=worldpoint_to_viewpoint(headPos);if onScreen and humanoid.Health>0 then local distance=localHeadPos and math.floor((localHeadPos-headPos).Magnitude)or 0;local hp=math.floor(humanoid.Health);local maxHp=math.floor(humanoid.MaxHealth);labels.NameTag.Position=screenPos+Vector2.new(0,-10);labels.InfoTag.Position=screenPos+Vector2.new(0,5);labels.NameTag.Text=mob.Name;labels.InfoTag.Text=string.format("HP: %d/%d | %dm",hp,maxHp,distance);labels.NameTag.Color=Color3.new(1,0.8,0);labels.InfoTag.Color=Color3.new(1,1,1);labels.NameTag.Visible=true;labels.InfoTag.Visible=true else labels.NameTag.Visible=false;labels.InfoTag.Visible=false end end end;local function ToggleMobESP(value) mobEspEnabled=value;if mobEspEnabled then if workspace:FindFirstChild("Entities")then for _,mob in pairs(workspace.Entities:GetChildren())do addMobToRenderList(mob)end;mobEvents.Added=workspace.Entities.ChildAdded:Connect(addMobToRenderList)end;if not mobESPUpdateConn or not mobESPUpdateConn.Connected then mobESPUpdateConn=RunService.RenderStepped:Connect(updateMobESPDrawing)end else if mobESPUpdateConn then mobESPUpdateConn:Disconnect();mobESPUpdateConn=nil end;for mob,_ in pairs(mobLabels)do cleanupMobLabels(mob)end;mobLabels={};for key,conn in pairs(mobEvents)do if conn and conn.Connected then conn:Disconnect()end end;mobEvents={}end end;LeftGroupBox:AddToggle('Enable Mob ESP',{Text='Mobs',Default=false,Tooltip="Show info above mobs",Callback=ToggleMobESP});Library:OnUnload(function() ToggleMobESP(false)end) end;local watermarkConn_FPS;do local FrameTimer=tick();local FrameCounter=0;local FPS=0;watermarkConn_FPS=game:GetService('RunService').RenderStepped:Connect(function() FrameCounter+=1;if(tick()-FrameTimer)>=1 then FPS=FrameCounter;FrameTimer=tick();FrameCounter=0;end;local pingValue=game:GetService('Stats').Network.ServerStatsItem['Data Ping']:GetValue();Library:SetWatermark(('ZYG HUB [V3] | %d fps | %d ping'):format(math.floor(FPS),math.floor(pingValue)));end); end;local MenuGroup=Tabs['UI Settings']:AddLeftGroupbox('Menu');MenuGroup:AddButton('Unload',function() Library:Unload()end);MenuGroup:AddLabel('Menu bind'):AddKeyPicker('MenuKeybind',{Default='LeftAlt',NoUI=true,Text='Menu keybind'});local TogglesGroup=Tabs["UI Settings"]:AddLeftGroupbox('Toggles');TogglesGroup:AddToggle('Deepwoken Mod Join',{Text='Toggle Notification On Mod Join',Default=true,Tooltip='Notification On mod join',Callback=function(Value) getgenv().ModJoinNotifyEnabled=Value end});getgenv().ModJoinNotifyEnabled=true;TogglesGroup:AddToggle('Keybinds Toggle',{Text='Toggle Keybinds Menu',Default=true,Tooltip='Turns keybind menu on and off',Callback=function(Value) Library.KeybindFrame.Visible=Value end});Library.KeybindFrame.Visible=true;Library.ToggleKeybind=Options.MenuKeybind;ThemeManager:SetLibrary(Library);SaveManager:SetLibrary(Library);SaveManager:IgnoreThemeSettings();SaveManager:SetIgnoreIndexes({'MenuKeybind','KeybindsToggle','Deepwoken Mod Join'});ThemeManager:SetFolder('ZYG HUB [V3]');SaveManager:SetFolder('ZYG HUB [V3]/Script Settings');SaveManager:BuildConfigSection(Tabs['UI Settings']);ThemeManager:ApplyToTab(Tabs['UI Settings']);SaveManager:LoadAutoloadConfig();do local groupId=5212858;local modJoinLeaveConns={};local modJoinNotifyEnabled=getgenv().ModJoinNotifyEnabled;Toggles['Deepwoken Mod Join']:OnChanged(function() modJoinNotifyEnabled=Toggles['Deepwoken Mod Join'].Value end);local function playSound(soundId) pcall(function() local sound=Instance.new("Sound",workspace);sound.SoundId="rbxassetid://"..soundId;sound.Volume=5;sound:Play();game.Debris:AddItem(sound,5)end) end;local function checkPlayer(player) if player==Player then return end;task.spawn(function() local success,isInGroup=pcall(player.IsInGroup,player,groupId);if success and isInGroup then if modJoinNotifyEnabled then Library:Notify(player.Name.." (Moderator) has joined the game!",15,"Warning");playSound("134569069127046")end;if modJoinLeaveConns[player]then modJoinLeaveConns[player]:Disconnect()end;modJoinLeaveConns[player]=player.AncestryChanged:Connect(function(_,parent) if not parent then if modJoinNotifyEnabled then Library:Notify(player.Name.." (Moderator) has left the game!",15,"Info");playSound("126849058770164")end;if modJoinLeaveConns[player]then modJoinLeaveConns[player]:Disconnect()end;modJoinLeaveConns[player]=nil end end)end end) end;local playerAddedConn_MJ=Players.PlayerAdded:Connect(checkPlayer);local playerRemovingConn_MJ=Players.PlayerRemoving:Connect(function(player) if modJoinLeaveConns[player]then modJoinLeaveConns[player]:Disconnect();modJoinLeaveConns[player]=nil end end);for _,player in ipairs(Players:GetPlayers())do checkPlayer(player)end;Library:OnUnload(function() if playerAddedConn_MJ then playerAddedConn_MJ:Disconnect()end;if playerRemovingConn_MJ then playerRemovingConn_MJ:Disconnect()end;for _,conn in pairs(modJoinLeaveConns)do if conn and conn.Connected then conn:Disconnect()end end end) end;local function SetupKeybind(optionName,toggleName) local keybindOption=Options[optionName];local toggle=Toggles[toggleName];if keybindOption and toggle then keybindOption:OnClick(function() toggle:SetValue(not toggle.Value)end) else warn("Keybind setup failed for:",optionName,"->",toggleName)end end;SetupKeybind('SpeedHackKeybind','SpeedHack');SetupKeybind('JumpPowerKeybind','JumpPowerToggle');SetupKeybind('FlyKeybind','FlyToggle');SetupKeybind('NoClipKeybind','NoClipToggle');SetupKeybind('InfJumpKeybind','Inf Jump');SetupKeybind('AnchorKeybind','Anchor Body/Torso');SetupKeybind('VMKeybind','VMToggle');Library:OnUnload(function() if watermarkConn_FPS then watermarkConn_FPS:Disconnect()end;print("Zyg Hub Unloaded.");Library.Unloaded=true end) end;local success,err=pcall(Script_Main);if not success then warn("Zyg Hub Script encountered an error during execution:",err);end;
